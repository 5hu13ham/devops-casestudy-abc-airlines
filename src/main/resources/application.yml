server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

    url: jdbc:mysql://localhost:3306/devops 

    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  mvc:
    view:
      prefix: /
      suffix: .jsp
  messages:

    basename: validation
  

